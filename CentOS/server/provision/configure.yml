---

- hosts: all
  remote_user: vagrant

  roles:

    - apache-server
    - django-framework
    - django-app

  tasks:

    - name: Nearly done!
      shell: echo 'Placeholder for some closing notes'
      notify:
        - Restart Server